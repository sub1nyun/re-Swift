import UIKit

//super -> 부모 메소드 호출 시 사용

//복습
//클래스의 프로퍼티는
//class Man {
// 1. var age : Int = 1 -> 초기 값이 있거나
// 2. var age : Int? -> 옵셔널 변수로 선언
// 3. init() { -> 이니셜 라이저 메소드 쓰거나 age = 1 or self.age

//var kim : Man = Man() -> ()해야함

//인스턴스 초기화 하기

//클래스, 구조체, 열거형 인스턴스가 생성되는 시점에서 해야하는 초기화 작업
//모든 프로퍼티를 다 초기화시키는 생성자 -> designated initializer

